---
title: "LabBook"
date: "`r Sys.Date()`"
output: 
  html_document:
    css: myStyle.css
    highlight: haddock
    toc: true
    toc_float: true
    number_sections: false
    code_folding: show
    df_print: paged
---

```{r global_options, include = FALSE}
knitr::opts_chunk$set(
  echo = TRUE,
  cache = TRUE,
  message = FALSE, # prevents messages that are generated by code from appearing in the finished file
  warning = FALSE, # prevents warnings that are generated by code from appearing in the finished
  error = TRUE, # if FALSE, knitr will not display any error messages generated by the code
  eval = TRUE, # if FALSE, knitr will not run the code in the code chunk
  include = TRUE, # f FALSE, knitr will run the chunk but not include the chunk in the final document
  results = 'hide', # If 'hide', knitr will not display the code's results in the final document. If 'hold', knitr will delay displaying all output pieces until the end of the chunk. If 'asis', knitr will pass through results without reformatting them (useful if results return raw HTML, etc.)
  fig.show = 'asis', # If 'hide', knitr will generate the plots created in the chunk, but not include them in the final document. If 'hold', knitr will delay displaying the plots created by the chunk until the end of the chunk. 
  fig.keep = 'last',
  fig.align = 'center',
  fig.width = 4.5,
  fig.height = 3,
  max.print = 3, # max no. of lines in data frames
  strip.white = TRUE, # remove leading and lagging white lines
  comment = NA # to remove all hashes
  )  
```

# First tests

http://centrodedescargas.cnig.es/CentroDescargas/catalogo.do?Serie=LIDAR
http://www.ign.es/wcts-app/


```{r}
# devtools::install_github('tylermorganwall/rayshader')
pacman::p_load(rayshader)
pacman::p_load(rgdal)
pacman::p_load(proj4)
```


```{r}
fileName <- here::here('data', 'PNOA_MDT25_ETRS89_HU30_0014_LID.asc') 

inputFileHeader <- scan(fileName, nlines = 6, what = character()) %>% 
  matrix(ncol = 2, byrow = TRUE) %>% 
  as.data.frame() %>% 
  setNames(c('variable', 'value')) %>% 
  dplyr::mutate(value = as.numeric(as.character(value)))

mapMatrix <- read.table(fileName, skip = 6, header = FALSE) %>% 
    as.matrix()

# Rotate matrix
mapMatrix <- t(mapMatrix)

```


```{r}
mapMatrix %>%
  sphere_shade(sunangle = 45,
               texture = 'desert'
  ) %>%
  add_water(detect_water(mapMatrix, min_area = 50), color = 'imhof3') %>%
  plot_map()
```

```{r}
mapMatrix %>%
  sphere_shade(texture = 'desert') %>%
  add_water(detect_water(mapMatrix, min_area = 50), color = 'imhof3') %>%
  # add_shadow(ray_shade(elmat, zscale = 3, maxsearch = 300), 0.5) %>%
  # add_shadow(ambmat, 0.5) %>%
  plot_3d(mapMatrix, zscale = 10, fov = 0, theta = 135, zoom = 0.75, phi = 45, windowsize = c(1000, 800))
render_snapshot()
```


# Track

```{r}
trackTable <- read.csv(here::here('data', 'La_Airúa.csv')) %>% 
  dplyr::select(1:5) %>% 
  dplyr::mutate(time = as.POSIXct(time, format = '%Y-%m-%dT%H:%M:%S'))
```

# Overlay

https://wcmbishop.github.io/rayshader-demo/

## Define box bounds

```{r}
# Bounds
source(here::here('R', 'TransformCoordinates.R'))
pacman::p_load(leaflet)

## Define xy corners from IGN asc
cornerCoord.southWest.xy <- c(inputFileHeader[3, 2], inputFileHeader[4, 2]) 
cornerCoord.northEast.xy <- cornerCoord.southWest.xy + 
  c(inputFileHeader[1, 2], inputFileHeader[2, 2]) * inputFileHeader[5, 2]

## Transform to lonLat
cornerCoord.souttWest.lonLat <- TransformCoordinates(cornerCoord.southWest.xy, is.lonLat = F)
cornerCoord.northEast.lonLat <- TransformCoordinates(cornerCoord.northEast.xy, is.lonLat = F)


## Define bounding box with longitude/latitude coordinates
bbox <- list(
  p1 = as.list(cornerCoord.souttWest.lonLat),
  p2 = as.list(cornerCoord.northEast.lonLat)
)

## Show in map
leaflet() %>%
  addTiles() %>% 
  addRectangles(
    lng1 = bbox$p1$lon, lat1 = bbox$p1$lat,
    lng2 = bbox$p2$lon, lat2 = bbox$p2$lat,
    fillColor = "transparent"
  ) %>%
  fitBounds(
    lng1 = bbox$p1$lon, lat1 = bbox$p1$lat,
    lng2 = bbox$p2$lon, lat2 = bbox$p2$lat,
  )
```

## Get overlay

```{r}
# Overlay image

## Define image size
majorDim <- 600

### calculate aspect ration (width/height) from lat/long bounding box
aspectRatio <- abs((bbox$p1$lon - bbox$p2$lon) / (bbox$p1$lat - bbox$p2$lat))

### define dimensions
imageWidth <- ifelse(aspectRatio > 1, majorDim, majorDim*aspectRatio) %>% round()
imageHeigth <- ifelse(aspectRatio < 1, majorDim, majorDim/aspectRatio) %>% round()

## fetch overlay image
source(here::here('R', 'GetArcGISMapImage.R'))
imageFileName <- here::here('figs', 'overlay.png')
GetArcGISMapImage(bbox, 
                  map_type = 'World_Topo_Map', file = imageFileName,
                  width = imageWidth, height = imageHeigth, 
                  sr_bbox = 4326
)

overlayImage <- png::readPNG(imageFileName)
```


